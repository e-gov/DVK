/**
 * Contains classes for DEC-s rights system.
 * Most of the classes in current package are technically data access classes
 * (they have a direct counterpart as database tables and implement logic for
 * database communication).
 */
package dhl.users;
